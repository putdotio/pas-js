import axios from 'axios'

class ApiClient {
  constructor({ url }) {
    this.baseURL = url
  }

  sendEvent() {
  }

  alias(currentId, nextId) {
  }

  identify(user) {
  }
}

export default ApiClient

